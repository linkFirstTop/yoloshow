name: yoloshow
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  cupertino_icons: ^1.0.5
  flutter_svg: ^1.1.1+1
  riverpod: ^1.0.3
  hooks_riverpod: ^1.0.4
  easy_refresh: ^3.0.4+1
  avatar_stack: ^1.1.2
  persistent_bottom_nav_bar:
    git:
      url: https://github.com/joehenrique7/PersistentBottomNavBar
  dd_js_util: ^0.3.7
  flutter_screenutil: ^5.5.3+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/bg/
    - assets/svgs/
